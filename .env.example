# Leave PAIRING_NUMBER empty to login via QR code scan instead.
# If set, format: country code + number, no + and no spaces (e.g. 8801XXXXXXXXX)
PAIRING_NUMBER=

# Render sets PORT automatically — no need to set this manually on Render.
PORT=3000
